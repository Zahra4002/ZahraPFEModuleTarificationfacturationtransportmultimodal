namespace Domain.Enums
{
    public enum SegmentStatus
    {
        Planned = 0,      // Planifié
        InProgress = 1,   // En cours
        Completed = 2,    // Terminé
        Cancelled = 3     // Annulé
    }
}